# En este directorio se encuentra la API

+ En la carpeta `models` están los modelos trabajados en archivo de EDA y modelado.
+ Se encuentra también el archivo de requirements.txt
+ La api se encuentra en el archivo `main.py`
